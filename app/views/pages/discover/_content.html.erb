<%= render "shared/video_grid",
      videos: @summaries,
      title: "Discover Latest Videos",
      search: {
            route: summaries_path,
            placeholder: "Search videos...",
            param_name: "q" # optional, defaults to "q"
      },
      path: summaries_path,
      params: request.query_parameters,
      empty_message: "Oops! No videos found." %>
<div class="<%= local_assigns[:embedded] ? '' : 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 sm:py-8' %>">
  <div class="bg-white <%= local_assigns[:embedded] ? '' : 'rounded-lg shadow p-4 sm:p-6 border border-gray-100' %>">
    <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 mb-6">
      <h1 class="text-lg lg:text-xl font-semibold">Manage Users</h1>
      <%= link_to "New User", new_dev_user_path,
          class: "h-12 px-6 bg-purple-600 text-white text-base rounded-lg hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 transition-colors flex items-center justify-center" %>
    </div>

    <%= render "shared/user_switcher", users: @users %>
  </div>
</div>
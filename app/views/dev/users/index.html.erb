<div class="<%= local_assigns[:embedded] ? '' : 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8' %>">
  <div class="bg-white <%= local_assigns[:embedded] ? '' : 'rounded-lg shadow' %> p-4 lg:p-6">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-xl lg:text-lg font-semibold">Manage Users</h1>
      <%= link_to "New User", new_dev_user_path,
          class: "px-4 py-2 text-base bg-purple-600 text-white rounded-lg hover:bg-purple-700" %>
    </div>

    <%= render "shared/user_switcher", users: @users %>
  </div>
</div>
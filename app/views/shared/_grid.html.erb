<%# Grid Container Component
    Provides consistent grid layout with responsive columns
    Inspired by the home page design with modern card styling

    Usage:
    render "shared/grid" do
      Content goes here
    end

    Parameters:
    - gap: Integer (optional) - Gap size between items (default: 4)
    - classes: String (optional) - Additional CSS classes
%>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 <%= local_assigns[:classes] %>">
  <%= yield if block_given? %>
</div>
<!DOCTYPE html>
<html class="h-full">
  <%= render "shared/head" %>

  <body class="h-full">
    <div class="min-h-full flex flex-col">
      <%= render "shared/nav" %>
      <%= render "shared/toast" %>

      <!-- Main Content -->
      <main class="flex-1">
        <%= yield %>
      </main>
    </div>
  </body>
</html>

<%# Channels Index %>
<div class="space-y-6">
  <%# Channels Grid %>
  <%= render "shared/page_header",
        title: "Your Channels",
        subtitle: pluralize(@channels&.length, "channel") do %>

    <%= render "channels/index/content" %>
  <% end %>
</div>
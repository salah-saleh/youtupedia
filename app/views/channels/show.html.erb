<%# Channel Show Page %>
<div>
  <%# About Channel Section %>
  <div class="mb-6">
    <div class="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 shadow-sm p-6">
      <%# Channel Header %>
      <div class="mb-6">
        <h1 class="text-2xl font-bold mb-3 text-gray-900 dark:text-white">
          <%= @channel[:title] %>
        </h1>

        <div class="flex items-center text-gray-600 dark:text-gray-400 text-sm space-x-4">
          <span><%= number_with_delimiter(@channel[:subscriber_count]) %> subscribers</span>
          <span class="text-gray-400 dark:text-gray-600">•</span>
          <span><%= pluralize(@channel[:video_count], 'video') %></span>
        </div>
      </div>

      <%# Channel Actions %>
      <div class="flex items-center space-x-4 mb-6">
        <button class="flex items-center space-x-2 text-gray-600 dark:text-gray-400 hover:text-purple-600 dark:hover:text-purple-400 transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
          </svg>
          <span>Follow</span>
        </button>

        <div data-controller="copy">
          <button class="flex items-center space-x-2 text-gray-600 dark:text-gray-400 hover:text-purple-600 dark:hover:text-purple-400 transition-colors"
                  data-action="click->copy#copy"
                  data-copy-target="button"
                  data-copy-text-value="<%= channel_url(@channel[:channel_name]) %>">
            <%= render_icon :link, class: "w-5 h-5" %>
            <span>Copy Link</span>
          </button>
        </div>

        <button class="flex items-center space-x-2 text-gray-600 dark:text-gray-400 hover:text-purple-600 dark:hover:text-purple-400 transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/>
          </svg>
          <span>Share</span>
        </button>
      </div>

      <%# Channel Description %>
      <div>
        <h3 class="text-gray-500 dark:text-gray-400 text-sm font-medium uppercase mb-4">About Channel</h3>
        <p class="text-gray-800 dark:text-gray-200 leading-relaxed">
          <%= @channel[:description] %>
        </p>
      </div>
    </div>
  </div>

  <%# Channel Videos Grid %>
  <%= turbo_frame_tag "channel_videos_content", data: { turbo_action: "advance" } do %>
    <%= render "shared/video_grid",
          videos: @videos,
          title: "Channel Videos",
          path: channel_path(@channel[:channel_name]),
          empty_message: "No videos available for this channel." %>
  <% end %>
</div>
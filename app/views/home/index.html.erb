<div class="max-w-4xl mx-auto text-center">
  <h1 class="text-6xl font-bold mb-4">
    Watch less, learn more and fast
    <span class="block">from long videos</span>
  </h1>

  <p class="text-xl text-gray-600 mb-8">
    Get YouTube video summaries in a minute. Just paste the link below.
  </p>

  <div class="mb-8">
    <%= form_with url: summary_path(1), method: :get, class: "relative" do |f| %>
      <%= f.text_field :youtube_url,
          placeholder: "Enter YouTube video link here...",
          required: true,
          class: "w-full px-4 py-3 rounded-full border border-purple-300 focus:outline-none focus:border-purple-500" %>

      <%= f.submit "Submit",
          class: "absolute right-2 top-1/2 transform -translate-y-1/2 px-6 py-2 bg-purple-500 text-white rounded-full hover:bg-purple-600" %>
    <% end %>
  </div>

  <div class="space-y-2">
    <p class="text-gray-600">Quick Try:</p>
    <div class="flex flex-wrap gap-2 justify-center">
      <%= link_to "#", class: "inline-flex items-center px-3 py-1 bg-gray-100 rounded-full text-sm text-gray-700 hover:bg-gray-200" do %>
        <span class="mr-2">▶️</span> VVIP CAR COLLECTION OF BABAS AND...
      <% end %>

      <%= link_to "#", class: "inline-flex items-center px-3 py-1 bg-gray-100 rounded-full text-sm text-gray-700 hover:bg-gray-200" do %>
        <span class="mr-2">▶️</span> Jaha Katha Besi Manepade | Sindoora...
      <% end %>

      <%= link_to "#", class: "inline-flex items-center px-3 py-1 bg-gray-100 rounded-full text-sm text-gray-700 hover:bg-gray-200" do %>
        <span class="mr-2">▶️</span> Finally . . . New Swift Facelift 2024...
      <% end %>
    </div>
  </div>
</div>